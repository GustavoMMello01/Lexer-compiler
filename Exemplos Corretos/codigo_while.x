main { 
    var result ; 
    var a ;
    var b ;
    var c ;

    c = 2.0 ;
    b = 1.0 ; 
    a = 5.0 ;

    while ( a > b ) {
        if ( b > c ) {
            print ( b ) ;
        }
        elif ( c > b ) {
            print ( c ) ;
        }
        else {
            print ( b ) ;
        }
    }
}